---
name: web-ui
description: Load WEB.md. Use when changing React, Vite, TypeScript 7, TanStack, Tailwind, shadcn, PWA, Mini App chrome, FSD web/src, or browser OTel.
---

# web-ui

Frames, not guards. Owner text this turn wins. Versions live in
`build/stack-pin.json`. Catalogue: `standards/` next to this skill.

1. Confirm the worktree. This staging repo has no application source
   unless the owner opened the product remote.
2. Session, models, context, or spawn: edit `build/stack-pin.json`
   and `.codex/config.toml` together, then `just check`.
3. Open CORE.md, WEB.md, pin frontend.* / runtimes.typescript.
   QUALITY when proving. CONTRACTS for the generated client. AUTH
   for CSRF/initData. EDUCATION for Tiptap/echarts/LiveKit UI.
   CLIENTS when Mini App / Tauri chrome.
4. Do not create web/ only to hold a frame. Browser: GET
   /v1/auth/csrf, credentials include, no Authorization. PWA
   denylist `/v1` and `/otel`.
5. Open `build/stack-pin.json` for the exact version.

Do not load the rest of the catalogue. Do not copy `docs/research/` into
the change. Prefer `$hack-agent-standards:web-ui` (plugin). Repo alias
`$apply-stack-rule` still opens INDEX when the layer is unclear.

