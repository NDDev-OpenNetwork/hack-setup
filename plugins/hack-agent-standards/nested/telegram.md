# Telegram

Read the repository root `AGENTS.md`. This directory is the aiogram
sibling uv graph (`environments.telegram`). Mini App chrome is `web/`.

Open `$hack-agent-standards:native-clients`. Always CORE. QUALITY when
proving. Mixed: AUTH (initData Ed25519 on FastAPI; bot principal
`BOT_SERVICE_TOKEN`), WEB (Mini App).

One consumer, poll XOR webhook. FSM on `redis-durable`. Do not mount
this process on FastAPI. Do not put `BOT_TOKEN` in the API env.
