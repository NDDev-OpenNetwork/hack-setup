# Desktop

Read the repository root `AGENTS.md`. This directory is Tauri 2
(`desktop/src-tauri`) wrapping `web/`.

Open `$hack-agent-standards:native-clients`. Always CORE. QUALITY when
proving. Mixed: WEB (same Vite tree), AUTH (native Bearer, not cookie).

No Flutter desktop window. Tokens in the OS keyring via one Rust command.
