# Mobile

Read the repository root `AGENTS.md`. This directory is Flutter FSD
(`mobile/lib`). Desktop is Tauri, not Flutter.

Open `$hack-agent-standards:native-clients`. Always CORE. QUALITY when
proving. Mixed: CONTRACTS (dart-dio), AUTH (native PKCE + Bearer).

ARB `ru` / `kk` / `en` on the first user-facing string. Engine deep
links off. Generated Dart lives outside this package.
