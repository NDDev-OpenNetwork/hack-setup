# Web and PWA scope

Read the repository root AGENTS.md and applicable parent guidance. Resolve all following paths from the repository root. Load `docs/agent-standards/INDEX.md`, then the WEB, CONTRACTS and FORMATS standards. CORE, SERENA-WORKFLOW and QUALITY still apply.

Use pure React/Vite/TanStack and the generated API client. Keep server state in Query, identity-scoped caches and RU/KK/EN resources consistent. Native TypeScript compilation remains authoritative when Serena uses a different navigation engine. Verify changed interaction in the actual browser; no Next.js, duplicate handwritten DTOs or generated-client patches by habit.

Work only in your assigned worktree. Check the actual diff and report precise validation results and any unsupported capability.
