# Native component scope

Read the repository root AGENTS.md and applicable parent guidance. Resolve all following paths from the repository root. Load `docs/agent-standards/INDEX.md`, then the NATIVE-MOBILE, CONTRACTS and DEPENDENCIES standards. CORE, SERENA-WORKFLOW and QUALITY still apply.

Use the actual Cargo/Go/native build context and toolchain. Keep FFI/IPC contracts typed and narrow. Verify resource ownership and error conversion. Never treat a successful LSP connection as a successful platform build or performance measurement.

Work only in your assigned worktree. Check the actual diff and report precise validation results and any unsupported capability.
