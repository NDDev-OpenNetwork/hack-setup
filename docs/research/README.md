# Research archive

Not runtime law. Do not copy this tree into `AGENTS.md`, skills, hooks, or
the artifact gate.

Live sources of truth:

- `AGENTS.md`
- `plugins/hack-agent-standards/standards/`
- `build/stack-pin.json` / generated `build/stack-standard.md`
- `docs/adr/0001`–`0008`

`hackalemai-codex-serena/` is a 2026-09-19 reviewed pack. It describes a
Serena-first / `docs/agent-standards/` layout that this repository did not
adopt. Live frames are the Codex plugin, not that pack.

`ponytail/` is the upstream `DietrichGebert/ponytail` snapshot (MIT,
`e3ba2aa6`, cloned 2026-09-20, `.git` stripped). Reference originals for
the laziness-ladder ruleset and its per-host hook machinery. Our adapted
skills are `$hack-agent-workflow:hack-mode`, `ship-verify` and
`debt-ledger` — narrower, hackathon-shaped, no review/test phases.
